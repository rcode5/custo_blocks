= simple_form_for(@user) do |f|
  = f.error_notification

  .form-inputs
    = f.input :email, autofocus: true, hint: 'this is the hint', placeholder: 'rock the place'
    = f.input :name

  .form-actions
    = f.button :submit, class: 'btn btn-primary'
    '
    = link_to 'Back', users_path, class: 'btn'

